export enum ScopeTag{
    SMOKE = '@smoke',
    REGRESSION = '@regression',
    SANITY = '@sanity',
    E2E = '@e2e',
    API = '@api',
    UI = '@ui'
}

export enum ENVTag {
    DEV = '@dev',
    TEST = '@test',
    QA = '@qa',
    UAT = '@uat',
    DEMO = '@demo',
    STAGING = '@staging',
    PROD = '@prod'
}

export enum TenantTag {
    DEFAULT = '@default',
    ORANGE = '@ORANGE',
    GREEN = '@GREEN',
    BLUE = '@BLUE'
}